<% if resource.errors.any? %>
  <div id="error_explanation" class="alert alert-danger">
    <h2><%= pluralize(resource.errors.count, "error") %> prohibited this <%= resource_name.to_s.humanize.downcase %> from being saved:</h2>

    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
